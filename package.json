{
  "name": "fluxgrid",
  "version": "5.0.0",
  "description": "Responsive and customisable utility-based CSS grid built with Sass",
  "main": "src/scss/fluxgrid.scss",
  "style": "src/scss/fluxgrid.scss",
  "author": "Johan Mouchet <mouchet-johan@gmail.com> (https://johan-mouchet.com)",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/JohanMouchet/fluxgrid.git"
  },
  "keywords": [
    "flexbox",
    "grid",
    "flex",
    "fluxbox",
    "sass"
  ],
  "files": [
    "src/scss/!(docs).scss",
    "dist/*.css"
  ],
  "scripts": {
    "dev": "parcel index.html --open --out-dir dev/",
    "build": "parcel build src/scss/{fluxgrid,fluxgrid-debug}.scss --no-source-maps",
    "docs": "parcel build index.html --out-dir docs/",
    "lint": "stylelint src/**/*.scss",
    "format": "npm run lint -- --fix && prettier --write **/*.md",
    "deploy": "npm run format && npm run build && npm run docs"
  },
  "devDependencies": {
    "husky": "^4.2.5",
    "lint-staged": "^10.2.11",
    "parcel-bundler": "^1.12.4",
    "prettier": "^2.0.5",
    "sass": "^1.26.10",
    "stylelint": "^13.6.1",
    "stylelint-config-prettier": "^8.0.2",
    "stylelint-config-standard": "^20.0.0",
    "stylelint-prettier": "^1.1.2",
    "stylelint-scss": "^3.18.0"
  },
  "postcss": {
    "plugins": {
      "autoprefixer": true
    }
  },
  "browserslist": [
    "defaults",
    "ie 11"
  ],
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  },
  "lint-staged": {
    "src/**/*.scss": [
      "stylelint --fix"
    ],
    "**/*.md": [
      "prettier --write"
    ],
    "[src/*, index.html]": [
      "npm run docs"
    ]
  }
}
